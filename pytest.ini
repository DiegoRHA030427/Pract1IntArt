# Configuración de pytest.
# pythonpath = src  -> permite que las pruebas hagan "from fase1 import bfs"
#                      sin tener que mover archivos ni modificar sys.path.
# testpaths = tests -> pytest solo busca pruebas en la carpeta tests/.
[pytest]
pythonpath = src
testpaths = tests
